@model List<SalesWeb.ViewModels.SellerViewModels.GetSellerViewModel>
@{
    Layout = "_Layout";
}
<h2>SELLERS</h2>
<hr/>
<p>
    <a asp-action="Post" class="btn btn-default">
        <button>CREATE NEW SELLER</button>
    </a>
</p>
<hr/>
<table class="table table-striped table-hover">
    <thead>
    <tr class="success">
        <th>
            @Html.DisplayNameFor(model => model[0].SellerId)
        </th>
        <th>
            @Html.DisplayNameFor(model => model[0].Name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model[0].Email)
        </th>
        <th>
            @Html.DisplayNameFor(model => model[0].DocumentIdentificationNumber)
        </th>
        <th>
            @Html.DisplayNameFor(model => model[0].BirthDate)
        </th>
        <th></th>
    </tr>
    </thead>
    <tbody>
    @foreach (var item in Model)
    {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.SellerId)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Name.FirstName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Name.LastName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Email.Address)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.DocumentIdentificationNumber.Number)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.BirthDate)
            </td>
            <td>
                <a asp-action="Put" asp-route-id="@item.SellerId">EDIT</a>
                <a asp-action="GetById" asp-route-id="@item.SellerId">DETAILS</a>
                <a asp-action="Delete" asp-route-id="@item.SellerId">DELETE</a>
            </td>
        </tr>
    }
    </tbody>
</table>

@section Scripts {
    @{ await Html.RenderPartialAsync("_ValidationScriptsPartial"); }
}